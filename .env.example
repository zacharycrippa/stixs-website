# PostgreSQL (required for production)
# Example format:
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require"
DATABASE_URL="postgresql://replace-user:replace-password@replace-host:5432/replace-db?sslmode=require"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-secret"

# Seeded admin account (change before production)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="change-me-before-production"

# Optional email notifications for contact enquiries
RESEND_API_KEY="re_xxxxxxxxxxxxxxxxx"
ADMIN_NOTIFY_EMAIL="admin@example.com"
RESEND_FROM="Stixs 3D <notifications@yourdomain.com>"